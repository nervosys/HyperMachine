[package]
name = "hv2-gpu"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
keywords.workspace = true
categories.workspace = true
description = "HyperMachine GPU virtualization with Vulkan and WebGPU"

[features]
default = ["wgpu-backend", "vulkan-backend"]
wgpu-backend = ["dep:wgpu"]
vulkan-backend = ["dep:ash"]

[dependencies]
hv2-core = { path = "../hv2-core", version = "1.1.0" }

tokio = { workspace = true }
async-trait = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }

# GPU support (optional)
wgpu = { workspace = true, optional = true }
ash = { workspace = true, optional = true }

# VFIO GPU passthrough uses raw syscalls (ioctl/mmap/pread) on Linux only.
[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"

[lints]
workspace = true
