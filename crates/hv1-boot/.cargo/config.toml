# Build configuration for hv1-boot bare-metal hypervisor
#
# Build with:
#   cargo +nightly build -p hv1-boot

[build]
target = "x86_64-unknown-none"

[unstable]
build-std = ["core", "alloc"]

[target.x86_64-unknown-none]
rustflags = ["-Cpanic=abort"]
