// AI Agent Test Script
print("Hello from AI agent!");
print(`VM Name: ${vm_name}`);
print(`VM State: ${vm_state}`);
print(`vCPUs: ${vcpu_count}`);

let report = #{
    vm: vm_name,
    state: vm_state,
    healthy: true
};

report
